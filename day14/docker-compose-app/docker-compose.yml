services:

  nginx:
    image: nginx:latest
    container_name: compose-nginx
    ports:
      - "8085:80"
    volumes:
      - ./html:/usr/share/nginx/html


  web-test:
    image: httpd:latest
    container_name: compose-apache
    ports:
      - "8086:80"
